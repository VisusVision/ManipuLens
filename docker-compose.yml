version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - OLLAMA_URL=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway" # Docker içinden bilgisayarındaki local Ollama'ya erişim sağlar
