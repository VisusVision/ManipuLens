{
  "name": "manipulens-rehber",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/build-sozluk.mjs && node scripts/extract-code.mjs && astro build",
    "sozluk": "node scripts/build-sozluk.mjs",
    "pano": "node scripts/fetch-trello.mjs",
    "kod": "node scripts/extract-code.mjs",
    "kod:kontrol": "node scripts/extract-code.mjs --kontrol",
    "links": "node scripts/check-links.mjs",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
